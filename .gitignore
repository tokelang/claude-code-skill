# Platform binaries are downloaded + SHA256-verified by the NPX installer at install
# time (install/install.js) — never committed. Keep only the resolver wrapper script.
bin/*
!bin/tokelang-cli

# npm
node_modules/
npm-debug.log*

# editor / OS cruft
*.log
.DS_Store
*.swp
